{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": null,
  "routes": [
    {
      "src": "/api/webhook",
      "dest": "/api/webhook"
    },
    {
      "src": "/api/health",
      "dest": "/api/health"
    }
  ]
}
